datasource db {
  provider = "mongodb"
  url      = "mongodb://lego:rT7GEF7gRG8POh@mongo:27017/lego?authSource=lego&retryWrites=true&w=majority"
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["mongodb"]
}

